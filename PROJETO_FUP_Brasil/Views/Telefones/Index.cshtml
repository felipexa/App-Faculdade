@model IEnumerable<Remake_FUP.Models.Telefone>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model._Telefone)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Aluno)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Funcionario)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ChefiaId)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item._Telefone)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Aluno.Cpf)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Funcionario.Cpf)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ChefiaId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.TelefoneId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.TelefoneId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.TelefoneId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
